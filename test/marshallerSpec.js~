describe("Schema marshaller suite",function(){

    it("creating actual form...",function(){
	var mForm=new MaterialForm(schemaTable, "test");
	var builder=new FormBuilder(mForm);
	builder.marshallSchema();
	console.log(mForm.form);
    });
    
});
